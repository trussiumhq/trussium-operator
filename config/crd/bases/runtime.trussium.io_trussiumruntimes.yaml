---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.21.0
  name: trussiumruntimes.runtime.trussium.io
spec:
  group: runtime.trussium.io
  names:
    kind: TrussiumRuntime
    listKind: TrussiumRuntimeList
    plural: trussiumruntimes
    shortNames:
    - truntime
    singular: trussiumruntime
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .status.readyReplicas
      name: Ready
      type: integer
    - jsonPath: .spec.replicas
      name: Desired
      type: integer
    - jsonPath: .spec.provider.type
      name: Provider
      type: string
    - jsonPath: .spec.provider.model
      name: Model
      type: string
    - jsonPath: .spec.image.tag
      name: Version
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: TrussiumRuntime declares a Trussium runtime instance.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: Spec defines the desired runtime configuration.
            properties:
              image:
                description: Image selects the released Trussium runtime container
                  image.
                properties:
                  digest:
                    description: |-
                      Digest is a SHA-256 OCI image digest.

                      Exactly one of tag or digest must be supplied.
                    pattern: ^sha256:[a-f0-9]{64}$
                    type: string
                  pullPolicy:
                    default: IfNotPresent
                    description: PullPolicy controls when Kubernetes pulls the runtime
                      image.
                    enum:
                    - Always
                    - IfNotPresent
                    - Never
                    type: string
                  repository:
                    description: |-
                      Repository is the container image repository without a tag or digest.

                      Example: ghcr.io/trussium/trussium
                    maxLength: 255
                    minLength: 1
                    pattern: ^[A-Za-z0-9][A-Za-z0-9._:/-]*$
                    type: string
                  tag:
                    description: |-
                      Tag is an immutable or versioned image tag.

                      Exactly one of tag or digest must be supplied.
                    maxLength: 128
                    minLength: 1
                    pattern: ^[A-Za-z0-9_][A-Za-z0-9._-]{0,127}$
                    type: string
                required:
                - repository
                type: object
                x-kubernetes-validations:
                - message: exactly one of tag or digest must be specified
                  rule: has(self.tag) != has(self.digest)
              imagePullSecrets:
                description: |-
                  ImagePullSecrets references Secrets used to pull runtime images.

                  References are resolved in the TrussiumRuntime namespace.
                items:
                  description: NamedReference identifies a namespaced Kubernetes object
                    by name.
                  properties:
                    name:
                      description: Name is the name of the referenced object in the
                        TrussiumRuntime namespace.
                      maxLength: 253
                      minLength: 1
                      pattern: ^[a-z0-9]([-a-z0-9.]*[a-z0-9])?$
                      type: string
                  required:
                  - name
                  type: object
                maxItems: 20
                type: array
                x-kubernetes-list-map-keys:
                - name
                x-kubernetes-list-type: map
              provider:
                description: Provider selects the AI provider and model.
                properties:
                  baseURL:
                    description: |-
                      BaseURL overrides the provider API endpoint.

                      It is commonly used for Ollama and OpenAI-compatible private endpoints.
                    format: uri
                    maxLength: 2048
                    type: string
                  credentialsSecretRef:
                    description: |-
                      CredentialsSecretRef references the Secret key containing provider
                      credentials.

                      Credential values must never be embedded in the custom resource.
                    properties:
                      key:
                        description: Key is the key containing the credential value.
                        maxLength: 253
                        minLength: 1
                        pattern: ^[A-Za-z0-9._-]+$
                        type: string
                      name:
                        description: Name is the Secret name in the TrussiumRuntime
                          namespace.
                        maxLength: 253
                        minLength: 1
                        pattern: ^[a-z0-9]([-a-z0-9.]*[a-z0-9])?$
                        type: string
                    required:
                    - key
                    - name
                    type: object
                  model:
                    description: Model identifies the model requested from the provider.
                    maxLength: 255
                    minLength: 1
                    type: string
                  type:
                    description: Type selects the provider adapter.
                    enum:
                    - openai
                    - ollama
                    type: string
                required:
                - model
                - type
                type: object
              replicas:
                default: 1
                description: Replicas is the desired number of runtime pods.
                format: int32
                maximum: 1000
                minimum: 0
                type: integer
              resources:
                description: Resources defines runtime-container compute resource
                  requirements.
                properties:
                  claims:
                    description: |-
                      Claims lists the names of resources, defined in spec.resourceClaims,
                      that are used by this container.

                      This field depends on the
                      DynamicResourceAllocation feature gate.

                      This field is immutable. It can only be set for containers.
                    items:
                      description: ResourceClaim references one entry in PodSpec.ResourceClaims.
                      properties:
                        name:
                          description: |-
                            Name must match the name of one entry in pod.spec.resourceClaims of
                            the Pod where this field is used. It makes that resource available
                            inside a container.
                          type: string
                        request:
                          description: |-
                            Request is the name chosen for a request in the referenced claim.
                            If empty, everything from the claim is made available, otherwise
                            only the result of this request.
                          type: string
                      required:
                      - name
                      type: object
                    type: array
                    x-kubernetes-list-map-keys:
                    - name
                    x-kubernetes-list-type: map
                  limits:
                    additionalProperties:
                      anyOf:
                      - type: integer
                      - type: string
                      pattern: ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$
                      x-kubernetes-int-or-string: true
                    description: |-
                      Limits describes the maximum amount of compute resources allowed.
                      More info: https://kubernetes.io/docs/concepts/configuration/manage-resources-containers/
                    type: object
                  requests:
                    additionalProperties:
                      anyOf:
                      - type: integer
                      - type: string
                      pattern: ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$
                      x-kubernetes-int-or-string: true
                    description: |-
                      Requests describes the minimum amount of compute resources required.
                      If Requests is omitted for a container, it defaults to Limits if that is explicitly specified,
                      otherwise to an implementation-defined value. Requests cannot exceed Limits.
                      More info: https://kubernetes.io/docs/concepts/configuration/manage-resources-containers/
                    type: object
                type: object
              runtime:
                description: Runtime contains optional runtime configuration overrides.
                properties:
                  providerRequestTimeoutSeconds:
                    description: ProviderRequestTimeoutSeconds limits a complete provider
                      request.
                    format: int32
                    maximum: 3600
                    minimum: 1
                    type: integer
                  shutdownDrainTimeoutSeconds:
                    description: |-
                      ShutdownDrainTimeoutSeconds limits active-workload draining during
                      graceful shutdown.
                    format: int32
                    maximum: 3600
                    minimum: 1
                    type: integer
                  streamIdleTimeoutSeconds:
                    description: StreamIdleTimeoutSeconds limits the wait between
                      provider stream events.
                    format: int32
                    maximum: 3600
                    minimum: 1
                    type: integer
                type: object
              service:
                default: {}
                description: Service defines the Kubernetes Service exposed for the
                  runtime.
                properties:
                  port:
                    default: 9000
                    description: Port is the Service port used to expose the Trussium
                      HTTP API.
                    format: int32
                    maximum: 65535
                    minimum: 1
                    type: integer
                  type:
                    default: ClusterIP
                    description: Type is the Kubernetes Service type.
                    enum:
                    - ClusterIP
                    - NodePort
                    - LoadBalancer
                    type: string
                type: object
            required:
            - image
            - provider
            type: object
          status:
            description: Status reports the observed runtime state.
            properties:
              availableReplicas:
                description: AvailableReplicas is the number of runtime replicas currently
                  available.
                format: int32
                type: integer
              conditions:
                description: Conditions describe the current runtime state.
                items:
                  description: Condition contains details for one aspect of the current
                    state of this API Resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: type of condition in CamelCase or in foo.example.com/CamelCase.
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - type
                x-kubernetes-list-type: map
              currentImage:
                description: CurrentImage is the complete runtime image observed by
                  the controller.
                type: string
              endpoint:
                description: Endpoint is the Kubernetes Service endpoint for the runtime.
                type: string
              observedGeneration:
                description: |-
                  ObservedGeneration is the most recent resource generation processed by
                  the controller.
                format: int64
                type: integer
              readyReplicas:
                description: ReadyReplicas is the number of runtime replicas currently
                  ready.
                format: int32
                type: integer
            type: object
        required:
        - spec
        type: object
    served: true
    storage: true
    subresources:
      status: {}
